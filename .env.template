# Azure OpenAI Service Credentials
# These are required for embedding generation and language model completions.
AZURE_OPENAI_KEY="YOUR_AZURE_OPENAI_API_KEY"
AZURE_OPENAI_ENDPOINT="YOUR_AZURE_OPENAI_ENDPOINT_URL" # e.g., https://your-resource-name.openai.azure.com/
AZURE_OPENAI_API_VERSION="2023-07-01-preview" # Or your preferred API version
AZURE_OPENAI_API_TYPE="azure" # Should remain "azure"

# Azure OpenAI Model Names (Deployment Names in Azure AI Studio)
# These are the names of your deployments for specific models.
AZURE_OPENAI_EMBEDDING_MODEL="text-embedding-ada-002" # Your deployment name for text-embedding-ada-002
AZURE_OPENAI_COMPLETION_MODEL="gpt-35-turbo"        # Your deployment name for GPT-3.5-Turbo or GPT-4

# Azure Cognitive Search Credentials
# Required for vector search and document retrieval.
AZURE_SEARCH_ENDPOINT="YOUR_AZURE_COGNITIVE_SEARCH_ENDPOINT_URL" # e.g., https://your-search-service-name.search.windows.net
AZURE_SEARCH_KEY="YOUR_AZURE_COGNITIVE_SEARCH_ADMIN_KEY"
AZURE_SEARCH_INDEX="rag-vector-index" # Default index name, can be overridden or changed if needed

# Microsoft Bot Framework Credentials (Optional for local development with Emulator)
# Required for deploying the bot to Azure Bot Service and connecting to channels.
# If using the Bot Framework Emulator locally, these can often be left blank,
# but ensure your emulator settings match (no App ID/Password).
MICROSOFT_APP_ID=""
MICROSOFT_APP_PASSWORD=""

# Server Port (Optional)
# Default port for the bot server is 3978 if not specified.
# PORT=3978

# --- Instructions ---
# 1. Rename this file to ".env" in the root directory of the project.
# 2. Fill in the placeholder values (YOUR_..._KEY, YOUR_..._URL) with your actual Azure service credentials.
# 3. Ensure the model deployment names match what you have configured in Azure AI Studio.
# 4. Do NOT commit the .env file with your actual secrets to version control. Add .env to your .gitignore file.
#
# Example .gitignore entry:
# .env
# .pytest_cache/
# .mypy_cache/
# __pycache__/
# *.pyc
# *.pyo
# *.pyd
# .Python
# env/
# venv/
# .DS_Store
# dummy_test_document.pdf # If generated by ingest.py tests
# app/.cache/ # For RAGAS logs and other cache files
# .cache/ # General cache directory
# *.log
# site/
# *.egg-info/
# dist/
# build/
# *.sqlite3 # If using SQLite for cache
# *.db # General DB files
# .ruff_cache/
